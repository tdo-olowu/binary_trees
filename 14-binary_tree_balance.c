#include "binary_trees.h"

/**
 * binary_tree_balance - measures the balance factor of a bin tree
 * this is the
 * @tree: a pointer to the root of some subtree
 * Return: the balance factor
 */
int binary_tree_balance(const binary_tree_t *tree)
{
	return (0);
}
